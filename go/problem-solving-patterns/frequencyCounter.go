package main

/* Problem Statement:
Write a function called 'same', which accepts two integer slices.
The function should return true if every value in the slice has it's corresponding value squared in the second slice.
The frequency of values must be the same.

Examples:
same([]int{1, 2, 3}, []int{4, 1, 9}) => true
same([]int{1, 2, 3}, []int{1, 9}) => false
same([]int{1, 2, 1}, []int{4, 4, 1}) => false (must be same frequency)
*/

// Naive way:

// Frequency Counter Way:
